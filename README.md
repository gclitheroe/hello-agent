# hello-agent

A hello world web application in Go, using only the standard library.

## Run

```
go run . -addr :8080
```

Then open http://localhost:8080. `-addr` defaults to `:8080`.

## Test

```
go test ./...
```
