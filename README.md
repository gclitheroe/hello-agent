# hello-agent

A simple "Hello, World!" program written in Go using only the standard library.

## Requirements

- Go 1.24.13 or higher

## Running the Program

To run the program directly:
```bash
go run main.go
```

To build and run the binary:
```bash
go build -o hello
./hello
```

## Output

```
Hello, World!
```
